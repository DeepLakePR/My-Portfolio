# My Portfolio v2

## Scripts

- `yarn dev`: run local dev server
- `yarn build`: create production build
- `yarn preview`: preview production build

## Structure

- `public/legacy`: old static version preserved
- `src`: new Vite + React app
- `src/components/ui`: reusable animated UI helpers
- `src/pages`: page-level sections
